[package]
name = "earth_evm_tooling"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ethers = { version = "2.0.14", features = ["abigen", "rustls"] }
hex = "0.4"

[lib]
path = "src/lib.rs"
